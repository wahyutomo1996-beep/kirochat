version: '3.8'

services:
  prometheus:
    build: .
    container_name: prometheus
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/app/data/prometheus.db
      - JWT_SECRET=${JWT_SECRET}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - NEXT_PUBLIC_APP_NAME=Prometheus
    volumes:
      - prometheus-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/login"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  prometheus-data:
    driver: local
