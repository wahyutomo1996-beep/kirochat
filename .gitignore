node_modules/
.next/
prisma/dev.db
.env
*.log

tsconfig.tsbuildinfo
# Workflow files (added manually via web UI - PAT lacks workflow scope)
.github/

# Session continuity files — may contain credential references, prod URLs,
# DB ids, etc. Project-scoped checkpoints stay local; backup script in
# C:\Users\WAHANA 25\Documents\opencode-backups\ handles archival.
SESSION-CHECKPOINT.md
SESSION-RESUME.md
